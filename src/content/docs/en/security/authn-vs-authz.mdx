---
title: Authentication vs Authorization
description: 'Who you are versus what you may do — and why 401 differs from 403.'
sidebar:
  order: 2
---

import NodeRunner from '../../../../components/NodeRunner.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

Two words that sound alike and do different jobs. Getting them straight clarifies both your code and your status codes.

## The difference

- **Authentication (authn)** — *who are you?* The client proves its identity with a credential: an API key, a session cookie, or a bearer token.
- **Authorization (authz)** — *what are you allowed to do?* Given a known identity, the server decides whether this caller may perform this action on this resource.

Authentication comes first; authorization builds on its result. A request can be authenticated (we know who you are) yet unauthorized (you still may not do that).

## Their status codes

- `401 Unauthorized` — authentication failed or was missing. The client should supply or refresh credentials. Conventionally accompanied by a `WWW-Authenticate` header.
- `403 Forbidden` — authentication succeeded, but the caller lacks permission. Sending different credentials will not help.

## Carrying the credential

Identity travels in the `Authorization` header:

```http
GET /me HTTP/1.1
Authorization: Bearer eyJhbGciOiJI...
```

## A minimal auth gate

export const honoCode = `const KEYS = new Set(['key-abc', 'key-def']);
const ADMIN = new Set(['key-abc']);

// authn: is the key valid?
app.use('*', async (c, next) => {
  const key = c.req.header('Authorization')?.replace('Bearer ', '');
  if (!key || !KEYS.has(key)) {
    return c.json({ title: 'Unauthorized', status: 401 }, 401, { 'WWW-Authenticate': 'Bearer' });
  }
  c.set('key', key);
  await next();
});

// authz: is this key allowed here?
app.delete('/articles/:id', (c) => {
  if (!ADMIN.has(c.get('key'))) {
    return c.json({ title: 'Forbidden', status: 403 }, 403);
  }
  return c.body(null, 204);
});`;

<NodeRunner stackblitz client:visible code={honoCode} />

<Callout title="401 means 'log in'; 403 means 'stop asking'">
If the caller is not authenticated, return `401` so a client knows to obtain or refresh a credential. If the caller is authenticated but not permitted, return `403` — re-authenticating will not change the answer. Conflating the two sends clients into pointless retry loops.
</Callout>

export const quiz = [
  {
    q: 'Which question does authorization answer?',
    options: ['Who are you?', 'What are you allowed to do?', 'Where are you?', 'When did you log in?'],
    answer: 1,
  },
  {
    q: 'A valid token is sent, but the user may not delete this resource. Which status?',
    options: ['401 Unauthorized', '403 Forbidden', '404 Not Found', '400 Bad Request'],
    answer: 1,
  },
  {
    q: 'A request arrives with no credentials at all. Which status?',
    options: ['200 OK', '401 Unauthorized', '403 Forbidden', '409 Conflict'],
    answer: 1,
  },
];

<Quiz client:visible id="security/authn-vs-authz" questions={quiz} />

<ProgressTracker client:visible id="security/authn-vs-authz" />
