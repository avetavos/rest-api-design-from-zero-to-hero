---
title: Caching และ ETags
description: 'Cache-Control, validator, และ 304 Not Modified เพื่อหลีกเลี่ยงการทำงานซ้ำ.'
sidebar:
  order: 4
---

import NodeRunner from '../../../../components/NodeRunner.tsx';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

HTTP มีโมเดล caching ที่ทรงพลังติดตัวมาอยู่แล้ว ถ้าใช้เป็น client (หรือ CDN) จะไม่ต้องดาวน์โหลดข้อมูลที่ไม่ได้เปลี่ยนซ้ำอีก และนี่มักเป็นการเพิ่ม performance ก้อนใหญ่ที่สุดที่ API หนึ่งตัวจะได้รับ

## Cache-Control: นี่จะ fresh นานแค่ไหน?

`Cache-Control` บอก cache ว่า response สามารถนำกลับมาใช้ซ้ำได้หรือไม่และนานเท่าใด:

```http
Cache-Control: public, max-age=60        # any cache may reuse for 60s
Cache-Control: private, max-age=0        # only the client, must revalidate
Cache-Control: no-store                  # never cache (sensitive data)
```

ตราบใดที่ response ยัง *fresh* อยู่ client จะหยิบของเดิมมาใช้ซ้ำโดยไม่ส่ง request เลย ต่อเมื่อกลายเป็น stale แล้วจึงจะ revalidate

## ETag + If-None-Match: ข้อมูลเปลี่ยนหรือยัง?

`ETag` คือ validator — ลายนิ้วมือของ representation ปัจจุบัน ใน request ถัดไป client จะส่งค่านี้กลับมาใน `If-None-Match` ถ้ายังตรงกันอยู่ server จะคืน `304 Not Modified` โดยไม่มี body:

export const flowDiagram = `sequenceDiagram
  participant C as Client
  participant S as Server
  C->>S: GET /articles/42
  S-->>C: 200 OK + ETag: "v7"
  Note over C: later...
  C->>S: GET /articles/42 (If-None-Match: "v7")
  S-->>C: 304 Not Modified (no body)`;

<Mermaid code={flowDiagram} title="ETag ที่ตรงกันเปลี่ยน response เต็มรูปแบบให้กลายเป็น 304 เล็ก ๆ" />

`Last-Modified` + `If-Modified-Since` คือคู่เทียบเท่าที่อิง timestamp แต่ ETag แม่นยำกว่า เพราะจับการเปลี่ยนแปลงได้ทุกแบบ ไม่ใช่แค่ที่ละเอียดถึงระดับวินาที

## การคำนวณ ETag

export const etagCode = `function etagFor(resource) {
  const json = JSON.stringify(resource);
  let hash = 0;
  for (let i = 0; i < json.length; i++) hash = (hash * 31 + json.charCodeAt(i)) | 0;
  return '"' + (hash >>> 0).toString(16) + '"';
}

const article = { id: 42, title: 'Hello', version: 7 };
const tag = etagFor(article);
console.log('ETag:', tag);

// Conditional request: client sends back the tag it has.
function handle(ifNoneMatch) {
  return ifNoneMatch === tag ? 304 : 200;
}
console.log('with stale tag:', handle('"old"'));   // 200
console.log('with current tag:', handle(tag));      // 304`;

<NodeRunner client:visible code={etagCode} />

| Cache Mechanism | ทำงานอย่างไร | เหมาะกับ |
|---|---|---|
| `Cache-Control: max-age=3600` | browser cache นาน 1 ชั่วโมง | static resource, slow-changing data |
| ETag + `If-None-Match` | server return 304 ถ้า resource ไม่เปลี่ยน | resource ที่เปลี่ยนบ้าง |
| `Last-Modified` + `If-Modified-Since` | ใช้ timestamp แทน hash | resource ที่รู้ modified time ชัดเจน |
| `Cache-Control: no-store` | ไม่ cache เลย | sensitive data เช่น payment, user profile |

## ข้อผิดพลาดที่พบบ่อย

**Cache Sensitive Data โดยไม่ตั้งใจ**
อาการ:
- `Cache-Control: max-age=3600` บน `/users/\{id\}/profile`
- browser หรือ CDN cache profile ของ user A ให้ user B เห็น
- ตั้ง `Cache-Control: no-store, private` สำหรับ authenticated resource

**ETag ที่ไม่ Stable**
อาการ:
- ETag generate จาก timestamp — server ที่ต่างกัน generate ETag ต่างกันสำหรับ content เดียว
- client ส่ง `If-None-Match` แต่ server return 200 ทุกครั้งแทน 304
- generate ETag จาก hash ของ content แทน timestamp

> 💡 ตัวอย่างจากของจริง
>
> **GitHub API:**
> - ETag บน resource ทุกตัว — conditional GET หลีกเลี่ยงการโอนข้อมูลที่ไม่เปลี่ยนซ้ำ
> - `304` บน authenticated request ไม่นับเข้า rate limit
>
> **AWS S3:**
> - ETag คือ MD5 hash ของ object content (สำหรับ upload ที่ไม่ใช่ multipart หรือเข้ารหัสด้วย SSE-KMS)
> - conditional GET ผ่าน `If-None-Match` ทำให้ sync ไฟล์จำนวนมากได้อย่างมีประสิทธิภาพ
>
> แหล่งอ้างอิง: [GitHub REST API — best practices](https://docs.github.com/en/rest/using-the-rest-api/best-practices-for-using-the-rest-api), [AWS S3 Object metadata (ETag)](https://docs.aws.amazon.com/AmazonS3/latest/API/API_Object.html)

<Callout title="ตั้ง Cache-Control บนทุก response">
แม้แต่ `Cache-Control: no-store` ก็เป็นการตัดสินใจที่ควรเขียนออกมาให้ชัด เพราะบอก cache ตรง ๆ ว่าต้องทำอะไร ดีกว่าปล่อยให้คลุมเครือ ให้ใส่ `ETag` กับ representation ที่คำนวณแพงหรือส่งข้อมูลหนัก จากนั้น conditional request จะเปลี่ยนการเช็ก "ข้อมูลยังเป็นปัจจุบันไหม" ส่วนใหญ่ให้กลายเป็น `304` ที่ราคาถูก
</Callout>

export const quiz = [
  {
    q: 'response แบบ 304 Not Modified มีอะไรอยู่ข้างใน?',
    options: [
      'resource ฉบับอัปเดตแบบเต็ม',
      'ไม่มี body — client นำสำเนาที่ cache ไว้กลับมาใช้ซ้ำ',
      'ข้อความ error',
      'URL สำหรับ redirect',
    ],
    answer: 1,
  },
  {
    q: 'header ใดที่นำ validator ซึ่ง client ส่งกลับมาเพื่อ revalidate?',
    options: ['If-None-Match (พร้อม ETag)', 'Cache-Control', 'Accept', 'Authorization'],
    answer: 0,
  },
  {
    q: 'directive ของ Cache-Control ใดที่หมายความว่า "อย่าจัดเก็บ response นี้เลย"?',
    options: ['max-age=0', 'public', 'no-store', 'private'],
    answer: 2,
  },
];

<Quiz client:visible id="versioning-caching/caching-etags" questions={quiz} />

<ProgressTracker client:visible id="versioning-caching/caching-etags" />
