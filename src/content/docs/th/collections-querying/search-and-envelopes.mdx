---
title: Search & Envelopes
description: 'Full-text search และ wrapper ที่สม่ำเสมอสำหรับทุก response แบบ collection'
sidebar:
  order: 5
---

import NodeRunner from '../../../../components/NodeRunner.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

ของสองอย่างปิดท้ายที่ทำให้ endpoint แบบ collection ใช้งานลื่นขึ้น คือ parameter สำหรับ search แบบ free-text และรูปร่าง envelope ชุดเดียวที่ทุก collection ใช้ร่วมกัน

## Search เทียบกับ structured filter

parameter `?q=` มีไว้สำหรับการ search แบบ fuzzy ที่เป็น free-text ทั่วทั้ง resource ("อะไรก็ตามที่ตรงกับ *graphql*"):

```http
GET /articles?q=graphql
```

structured filter (`?status=published`) ตอบคำถามที่ชัดเจน ส่วน search ตอบคำถามที่คลุมเครือ ทั้งสองทำงานร่วมกันได้ — `?q=graphql&status=published` คือการ search ภายในชุดที่ filter มาแล้ว แต่ต้องแยกบทบาทให้ชัด: `q` เป็น fuzzy มีการจัดอันดับ (ranked) และอาจมี search engine อยู่เบื้องหลัง ส่วน filter เป็น predicate ที่แม่นยำ

## envelope ที่สม่ำเสมอ

ทุก response แบบ collection ควรใช้ wrapper เดียวกัน เพื่อให้ client เขียน code จัดการการแบ่งหน้าเพียงครั้งเดียว:

export const envelopeCode = `function envelope(data, { page, limit, total, basePath }) {
  return {
    data,
    meta: { page, limit, total },
    links: {
      self: basePath + '?page=' + page,
      next: page * limit < total ? basePath + '?page=' + (page + 1) : null,
      prev: page > 1 ? basePath + '?page=' + (page - 1) : null,
    },
  };
}

const rows = [{ id: 1 }, { id: 2 }];
console.log(JSON.stringify(envelope(rows, { page: 1, limit: 2, total: 7, basePath: '/articles' }), null, 2));`;

<NodeRunner client:visible code={envelopeCode} />

## ต้นทุนของการนับจำนวนรวม

การคืน `total` นั้นสะดวก แต่บนตารางขนาดใหญ่ การนับทุกแถวที่ตรงเงื่อนไขอาจมีต้นทุนพอ ๆ กับตัว query เอง ทางเลือก: cache จำนวนนับไว้ คืนจำนวนแบบโดยประมาณ หรือไม่คืนเลย (cursor pagination มักทำแบบนี้ — ให้แค่ `next` โดยไม่มียอดรวม)

| ข้อดี (Response Envelope) | ข้อแลกเปลี่ยน |
|---|---|
| metadata (pagination, total) รวมอยู่ใน response เดียว | verbose กว่า bare array |
| consistent structure ทุก collection endpoint | client ต้อง unwrap `data` ทุกครั้ง |
| ง่ายต่อการเพิ่ม metadata ในอนาคตโดยไม่ breaking | bare array ง่ายกว่าสำหรับ simple use case |
| search result มี `meta.total`, `meta.took` ใน envelope | |

## ข้อผิดพลาดที่พบบ่อย

**Search ที่ไม่ต่างจาก Filter**
อาการ:
- `GET /users?search=john` — query ทุก field ด้วย LIKE '%john%'
- slow บน large table, ไม่ rank ตาม relevance
- search จริงควรใช้ full-text search engine (Elasticsearch, PostgreSQL FTS)

**Envelope ที่ Inconsistent**
อาการ:
- `GET /users` return `{ "data": [...], "meta": {...} }`
- `GET /products` return `{ "items": [...], "pagination": {...} }`
- กำหนด envelope มาตรฐานและใช้ทุก collection endpoint

> 💡 ตัวอย่างจากของจริง
>
> **GitHub API:**
> - search: `GET /search/repositories?q=rest+api+language:javascript&sort=stars`
> - envelope: `{ "total_count": 12345, "incomplete_results": false, "items": [...] }`
>
> **Stripe API:**
> - envelope สม่ำเสมอ: `{ "object": "list", "data": [...], "has_more": true, "url": "/charges" }`
> - ทุก list endpoint ใช้ format เดียวกัน

<Callout title="envelope เดียว ใช้ได้ทุกที่">
เลือก envelope ของ collection สักแบบ — `data` + `meta` + `links` เป็นค่าเริ่มต้นที่แน่นหนา — แล้วใช้รูปแบบเดียวกันนี้กับทุก endpoint ที่เป็น list รวมถึงผลลัพธ์ของ search ด้วย client จะได้เรียนรู้ pagination ของคุณเพียงครั้งเดียวแล้วใช้ซ้ำได้ทั่วทั้ง API และอย่าลืมว่าการนับ `total` ให้แม่นยำนั้นไม่ฟรี ถ้าเริ่มสร้างปัญหาก็ตัดทิ้งได้
</Callout>

export const quiz = [
  {
    q: '`?q=` ต่างจาก structured filter อย่าง `?status=published` อย่างไร?',
    options: [
      'เหมือนกันทุกประการ',
      '`q` เป็น free-text search แบบ fuzzy/ranked ส่วน filter เป็น predicate ที่แม่นยำ',
      '`q` ใช้ได้กับตัวเลขเท่านั้น',
      'filter รวมกับ q ไม่ได้',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมการคืนจำนวนนับ `total` จึงอาจมีต้นทุนสูง?',
    options: [
      'JSON เก็บตัวเลขขนาดใหญ่ไม่ได้',
      'การนับทุกแถวที่ตรงเงื่อนไขบนตารางขนาดใหญ่อาจมีต้นทุนพอ ๆ กับตัว query',
      'ต้องผ่าน authentication ก่อน',
      'ทำให้ pagination พัง',
    ],
    answer: 1,
  },
  {
    q: 'ประโยชน์ของการมี envelope ของ collection ที่ใช้ร่วมกันชุดเดียวคืออะไร?',
    options: [
      'response เล็กลง',
      'client เรียนรู้ pagination เพียงครั้งเดียวและนำกลับมาใช้ซ้ำได้ทั่วทุก endpoint ที่เป็น list',
      'ทำให้ไม่ต้องใช้ status code อีกต่อไป',
      'เปิดให้ GET แก้ไขข้อมูลได้',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="collections-querying/search-and-envelopes" questions={quiz} />

<ProgressTracker client:visible id="collections-querying/search-and-envelopes" />
