---
title: Idempotency & Safety
description: 'สองคุณสมบัติที่ทำให้ HTTP request retry ได้อย่างปลอดภัย — และทำไมจึงสำคัญ'
sidebar:
  order: 4
---

import NodeRunner from '../../../../components/NodeRunner.tsx';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

มีสองคุณสมบัติที่ตัดสินว่า request หนึ่งจะทำซ้ำได้อย่างปลอดภัยหรือไม่ สองตัวนี้สับสนกันง่ายมาก เรามาแยกให้ชัดกันก่อน

## Safe กับ idempotent

- **Safe** — request ไม่มีผลที่สังเกตได้ต่อ state ของ server ทำได้แค่อ่านอย่างเดียว ตัวอย่างเช่น `GET`, `HEAD` และ `OPTIONS`
- **Idempotent** — ส่ง request เดิม *N* ครั้งแล้ว server ลงเอยที่ state เดียวกับส่งครั้งเดียว `GET`, `HEAD`, `OPTIONS`, `PUT` และ `DELETE` เป็น idempotent ส่วน `POST` และ (โดยทั่วไป) `PATCH` ไม่เป็น

ทุก method ที่ safe ล้วน idempotent แต่ไม่ใช่ทุก method ที่ idempotent จะ safe — `DELETE` เปลี่ยนแปลง state แต่ก็ยัง idempotent

export const matrixDiagram = `flowchart TD
  M{Method} --> GET[GET / HEAD: safe + idempotent]
  M --> PUTDEL[PUT / DELETE: idempotent, not safe]
  M --> POST[POST: neither]
  M --> PATCH[PATCH: usually neither]`;

<Mermaid code={matrixDiagram} title="ตำแหน่งของแต่ละ method บนตาราง safe/idempotent" />

## ทำไมสองเรื่องนี้ถึงสำคัญ: การ retry

เครือข่ายทำ response หายได้ เมื่อ client (หรือ proxy หรือ load balancer) ไม่ได้รับการตอบกลับ ก็มีสิทธิ์ยิงซ้ำ ถ้า method เป็น idempotent การ retry ก็ไม่มีอันตราย แต่ถ้าไม่ใช่ — อย่าง `POST` ที่ตัดเงินจากบัตร — การ retry แบบมืดบอดอาจตัดเงินซ้ำสองครั้ง

export const retryCode = `// A PUT is idempotent: applying it twice == applying it once.
const store = { 42: { id: 42, status: 'draft' } };

function putStatus(id, status) {
  store[id] = { ...store[id], status };
  return store[id];
}

console.log(putStatus(42, 'published')); // first attempt
console.log(putStatus(42, 'published')); // retry after a dropped response
console.log('final state:', store[42]);   // same as one call`;

<NodeRunner client:visible code={retryCode} />

## ทำให้ POST retry ได้อย่างปลอดภัย

เมื่อคุณจำเป็นจริง ๆ ที่จะให้การสร้างที่ไม่ idempotent ทนต่อการ retry ได้ ให้ใช้ **idempotency key**: client ส่ง header `Idempotency-Key` ที่ไม่ซ้ำกันมา และ server จดจำผลลัพธ์ของ key นั้นไว้ เพื่อให้การส่งซ้ำคืนผลลัพธ์เดิมกลับไปแทนที่จะทำงานสองครั้ง รายละเอียดของกลไกนี้อยู่ใน [Versioning & Caching](/rest-api/th/versioning-caching/conditional-and-idempotency-keys/)

| Property | ความหมาย | Methods |
|---|---|---|
| Safe | ไม่เปลี่ยน state ของ server | GET, HEAD, OPTIONS |
| Idempotent | ทำซ้ำกี่ครั้งผลเดิม | GET, PUT, DELETE (+ safe methods) |
| Neither | อาจเปลี่ยน state และผลต่างกัน | POST, PATCH |

## ข้อผิดพลาดที่พบบ่อย

**POST ที่ไม่ Idempotent โดยไม่มี Guard**
อาการ:
- user กด submit form สองครั้ง — สร้าง order สองใบ
- network retry ทำให้ charge บัตรสองครั้ง
- ต้องการ idempotency key สำหรับ POST ที่ sensitive เช่น payment

**PATCH ที่ไม่ Idempotent โดยตั้งใจ**
อาการ:
- `PATCH /accounts/\{id\}/balance` ด้วย `{ "increment": 100 }` — ทำซ้ำ = บวกซ้ำ
- client retry ทำให้ balance เพิ่มผิด
- ใช้ absolute value: `{ "balance": 1100 }` แทน increment

> 💡 ตัวอย่างจากของจริง
>
> **Stripe API:**
> - `Idempotency-Key` header สำหรับ POST /charges
> - ส่ง key เดิมซ้ำ — Stripe return response เดิม ไม่ charge ซ้ำ
> - เก็บ key ใน client และ retry ได้อย่างปลอดภัย
>
> **AWS S3:**
> - PUT object เป็น idempotent — upload ไฟล์เดิมซ้ำ = overwrite ไม่ใช่ duplicate
> - ทำให้ retry ปลอดภัยสำหรับ large file upload
>
> แหล่งอ้างอิง: [Stripe idempotent requests](https://docs.stripe.com/api/idempotent_requests), [AWS S3 PutObject](https://docs.aws.amazon.com/AmazonS3/latest/API/API_PutObject.html)

<Callout title="ออกแบบเผื่อการ retry ที่คุณมองไม่เห็น">
ให้ตั้งสมมติฐานไว้เลยว่าทุก request ที่ไม่ safe อาจวิ่งมาถึงมากกว่าหนึ่งครั้ง จุดไหนทำ write ให้เป็น idempotent ได้ก็ทำ (เลือก `PUT` ด้วย ID ที่ client เป็นคนกำหนด) ส่วนจุดที่ทำไม่ได้ก็ป้องกันด้วย idempotency key ความถูกต้องภายใต้การ retry มาจากการออกแบบ ไม่ใช่โชค
</Callout>

export const quiz = [
  {
    q: 'ข้อความใดเป็นจริง?',
    options: [
      'ทุก method ที่ idempotent ก็ safe ด้วย',
      'ทุก method ที่ safe ก็ idempotent ด้วย',
      'POST เป็น idempotent',
      'DELETE เป็น safe',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมการ retry POST ที่ไม่ idempotent จึงมีความเสี่ยง?',
    options: [
      'ทำงานช้ากว่า',
      'อาจทำงานซ้ำสองรอบ (เช่น ตัดเงินซ้ำ)',
      'cache ไม่ได้',
      'คืน status code ผิด',
    ],
    answer: 1,
  },
  {
    q: 'อะไรที่ทำให้การสร้างที่ไม่ idempotent ทนต่อการ retry ได้อย่างปลอดภัย?',
    options: ['timeout ที่นานขึ้น', 'Idempotency-Key ที่ server จดจำไว้', 'ใช้ GET แทน', 'response แบบ 500'],
    answer: 1,
  },
];

<Quiz client:visible id="methods-crud-idempotency/idempotency-and-safety" questions={quiz} />

<ProgressTracker client:visible id="methods-crud-idempotency/idempotency-and-safety" />
